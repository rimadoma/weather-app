# This project tracks bleeding-edge versions (e.g. TypeScript 6) that run ahead
# of some tools' declared peer ranges -- openapi-typescript still pins its peer
# to typescript@^5.x, though it runs fine on 6. Accept those cosmetic peer
# mismatches rather than letting them block `npm install`.
legacy-peer-deps=true
